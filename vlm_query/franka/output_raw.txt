import numpy as np

# Number of stages
num_stages = 3

### Stage 1: Grasp the white pen
def stage1_subgoal_constraint1(end_effector, keypoints):
    """Align the end-effector with the white pen (keypoint 2)."""
    white_pen_kp = keypoints[2]
    cost = np.linalg.norm(end_effector - white_pen_kp)
    return cost

### Stage 2: Reorient the white pen upright
def stage2_subgoal_constraint1(end_effector, keypoints):
    """The white pen is upright (parallel to the z-axis)."""
    white_pen_kp = keypoints[2]
    delta_z = white_pen_kp[2]
    cost = np.abs(delta_z)  # Ensure the pen is upright along the z-axis
    return cost

def stage2_path_constraint1(end_effector, keypoints):
    """The robot must still be grasping the white pen."""
    return get_grasping_cost_by_keypoint_idx(2)

### Stage 3: Drop the white pen upright into the black pen holder
def stage3_subgoal_constraint1(end_effector, keypoints):
    """The white pen is 5cm above the black pen holder opening (keypoint 4)."""
    pen_holder_kp = keypoints[4]
    offsetted_point = pen_holder_kp + np.array([0, 0, 5])
    cost = np.linalg.norm(end_effector - offsetted_point)
    return cost

def stage3_path_constraint1(end_effector, keypoints):
    """The robot must still be grasping the white pen."""
    return get_grasping_cost_by_keypoint_idx(2)

# Summarize keypoints to be grasped in all grasping stages
grasp_keypoints = [2, -1, -1]

# Summarize at the end of which stage the robot should release the keypoints
release_keypoints = [-1, -1, 2]
